spring:
    application:
        name: slerp-io
    cloud:
        config:
            failFast: false
            discovery:
                enabled: true
                serviceId: configsvr

eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka/
