spring:
    application:
        name: slerp-io
    cloud:
        config:
            server:
                bootstrap: true
                git:
                    uri: https://github.com/kiditz/slerpio.git
                    searchPaths: 'config, config/{application}'
                    cloneOnStart: true
                    basedir: 'target/config'

---
spring:
    profiles: heroku
eureka:
    instance:
        hostname: belajar-springcloud-configsvr.herokuapp.com
