---
spring:
  profiles: docker
  application:
    name: slerp-io
  cloud:
    consul:
      host: http://192.168.99.100
      port: 8500
      config:
        fail-fast: true
        format: yaml
---      
spring:
  profiles: dev
  application:
    name: slerp-io
  cloud:
    consul:
      host: ${CONSUL_SERVER:http://192.168.99.100}
      port: 8500
      config:
        fail-fast: true
        format: yaml
      
