name: Slerp Product Service Development

spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: none
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/product-service
    username: postgres
    password: rioters7
    driverClassName: org.postgresql.Driver
