spring:
    application:
        name: configsvr
    cloud:
        config:
            server:
                bootstrap: true
                git:
                    uri: https://github.com/kiditz/slerpio.git
                    searchPaths: 'config, config/{application}'
                    cloneOnStart: true
                    basedir: 'target/config'

